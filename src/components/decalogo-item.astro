---
const { title } = Astro.props
---

<div class="decalogo-text border border-gray-500 bg-black/20 p-5">
  <h1 class="pb-4 font-semibold">{title}</h1>
  <div class="font-light">
    <slot />
  </div>
</div>

<style>
  .decalogo-text {
    border-radius: 5px;
    animation: reveal-decalogo auto both;
    animation-timeline: view();
    animation-range: entry 10% cover 20%;
  }
  @keyframes reveal-decalogo {
    from {
      opacity: 0;
      translate: 0 100px;
      scale: 0.5;
    }
    to {
      opacity: 1;
      translate: 0 0;
      scale: 1;
    }
  }
</style>
